# Keystore 签名配置文件
#
# 使用说明：
# 1. 复制此文件为 keystore.properties
# 2. 填写您的实际签名信息
# 3. 将 keystore.properties 添加到 .gitignore（重要！不要提交到版本控制）
#
# 生成 Keystore 的命令：
# keytool -genkey -v -keystore release.keystore -alias healthcenter -keyalg RSA -keysize 2048 -validity 10000
#
# 参数说明：
# -keystore: 指定生成的 keystore 文件名
# -alias: 指定别名
# -keyalg: 加密算法，使用 RSA
# -keysize: 密钥长度，2048位
# -validity: 有效期，10000天（约27年）

# Keystore 文件路径（相对于 app 目录）
storeFile=release.keystore

# Keystore 密码
storePassword=your_store_password_here

# Key 别名
keyAlias=healthcenter

# Key 密码
keyPassword=your_key_password_here
